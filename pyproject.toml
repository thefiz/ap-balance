[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ap-balance"
version = "0.2.0"
description = "Archipelago YAML progression and sphere analysis prototype"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
apbalance = "apbalance.cli:main"

[tool.setuptools.packages.find]
include = ["apbalance*"]
